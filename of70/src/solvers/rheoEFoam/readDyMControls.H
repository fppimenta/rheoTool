#include "readTimeControls.H"
 
 correctPhi = simple.dict().lookupOrDefault
 (
     "correctPhi",
     correctPhi
 );
 
 checkMeshCourantNo = simple.dict().lookupOrDefault
 (
     "checkMeshCourantNo",
     checkMeshCourantNo
 );
 
 moveMeshOuterCorrectors = simple.dict().lookupOrDefault
 (
     "moveMeshOuterCorrectors",
     moveMeshOuterCorrectors
 );
